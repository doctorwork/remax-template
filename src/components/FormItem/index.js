import React from "react";
import { View, Label, Input } from "remax/base";

export default props => {
  return (
    <View className="form-item">
      <Label>TEST</Label>
      <Input value="" />
    </View>
  );
};
